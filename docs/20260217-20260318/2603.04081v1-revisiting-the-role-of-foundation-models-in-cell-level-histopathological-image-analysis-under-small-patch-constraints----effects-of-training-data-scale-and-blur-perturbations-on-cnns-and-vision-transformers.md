---
title: Revisiting the Role of Foundation Models in Cell-Level Histopathological Image Analysis under Small-Patch Constraints -- Effects of Training Data Scale and Blur Perturbations on CNNs and Vision Transformers
title_zh: 重新审视小切片限制下基础模型在细胞级组织病理图像分析中的作用——训练数据规模和模糊扰动对 CNN 和 Vision Transformer 的影响
authors: "Hiroki Kagiyama, Toru Nagasaka, Yukari Adachi, Takaaki Tachibana, Ryota Ito, Mitsugu Fujita, Kimihiro Yamashita, Yoshihiro Kakeji"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04081v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 用于结直肠癌细胞级组织病理图像分析的基础模型
tldr: 本研究探讨了在40x40像素极小图像块约束下，细胞级病理图像分析中基础模型与特定任务架构的性能差异。通过对18.5万张结直肠癌细胞图像进行实验，对比了8种从零训练的模型与3种基础模型在不同数据规模和模糊扰动下的表现。结果表明，针对小图像块优化的CustomViT在准确率和推理成本上均优于基础模型，且基础模型在数据规模增加时更易达到性能瓶颈。
selection_source: fresh_fetch
motivation: 评估在极小分辨率（40x40）约束下，基础模型与特定任务架构在细胞级病理图像分析中的鲁棒性与可扩展性。
method: 基于18.5万张标注细胞图像，对比了多种从零训练的CNN/ViT架构与经过尺寸调整的预训练基础模型在不同数据量下的表现。
result: 针对小图像块优化的CustomViT准确率最高，且基础模型在数据规模扩大时性能提升有限，容易出现饱和。
conclusion: 在极小图像块的细胞分类任务中，当数据充足时，特定任务架构比基础模型更具效能和效率，且基础模型并无鲁棒性优势。
---

## 摘要
背景与目标：细胞级病理图像分析需要处理极小的图像切片（40x40 像素），远低于标准的 ImageNet 分辨率。目前尚不清楚现代深度学习架构和基础模型是否能在这种限制下学习到鲁棒且可扩展的表征。我们系统地评估了小切片细胞分类的架构适用性和数据规模效应。方法：我们分析了 303 份具有 CD103/CD8 免疫组化染色的结直肠癌标本，生成了 185,432 张带标注的细胞图像。我们在多个数据规模（FlagLimit：每类 256-16,384 个样本）下从头训练了八种特定任务架构，并在将输入调整为 224x224 像素后，通过线性探测和微调评估了三种基础模型。使用调整尺寸前后的高斯扰动评估了对模糊的鲁棒性。结果：特定任务模型随着数据规模的增加而持续改进，而基础模型在适度的样本量下就达到了饱和。一种针对小切片优化的 Vision Transformer（CustomViT）实现了最高的准确率，在推理成本大幅降低的情况下优于所有基础模型。各架构之间的模糊鲁棒性相当，未观察到基础模型具有定性优势。结论：对于极端空间限制下的细胞级分类，一旦有足够的训练数据，特定任务架构比基础模型更有效且更高效。较高的洁净准确率并不意味着更优的鲁棒性，大型预训练模型在小切片领域的收益有限。

## Abstract
Background and objective: Cell-level pathological image analysis requires working with extremely small image patches (40x40 pixels), far below standard ImageNet resolutions. It remains unclear whether modern deep learning architectures and foundation models can learn robust and scalable representations under this constraint. We systematically evaluated architectural suitability and data-scale effects for small-patch cell classification. Methods: We analyzed 303 colorectal cancer specimens with CD103/CD8 immunostaining, generating 185,432 annotated cell images. Eight task-specific architectures were trained from scratch at multiple data scales (FlagLimit: 256--16,384 samples per class), and three foundation models were evaluated via linear probing and fine-tuning after resizing inputs to 224x224 pixels. Robustness to blur was assessed using pre- and post-resize Gaussian perturbations. Results: Task-specific models improved consistently with increasing data scale, whereas foundation models saturated at moderate sample sizes. A Vision Transformer optimized for small patches (CustomViT) achieved the highest accuracy, outperforming all foundation models with substantially lower inference cost. Blur robustness was comparable across architectures, with no qualitative advantage observed for foundation models. Conclusion: For cell-level classification under extreme spatial constraints, task-specific architectures are more effective and efficient than foundation models once sufficient training data are available. Higher clean accuracy does not imply superior robustness, and large pre-trained models offer limited benefit in the small-patch regime.

---

## 论文详细总结（自动生成）

这是一份关于论文《Revisiting the Role of Foundation Models in Cell-Level Histopathological Image Analysis under Small-Patch Constraints》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在数字病理学中，单细胞级别的分析通常受限于极小的图像空间（如 40×40 像素），这远低于 ImageNet 标准的 224×224 分辨率。
*   **核心问题**：目前学术界倾向于使用大型预训练基础模型（Foundation Models），但这些模型在面对极小切片（Small-Patch）约束时，其表征能力、可扩展性以及相对于特定任务架构（Task-Specific Architectures）的优势是否依然存在，目前尚缺乏系统性研究。
*   **整体含义**：本研究旨在重新评估在极端空间限制下，不同深度学习架构（CNN vs. ViT）以及基础模型在病理细胞分类任务中的实际效能、数据效率和鲁棒性。

### 2. 方法论
*   **核心思想**：对比“从零训练的轻量化特定任务模型”与“基于插值缩放的基础模型”在不同数据规模下的表现。
*   **关键技术细节**：
    *   **特定任务架构**：实现了 8 种模型，包括 MLP、标准 CNN、ResNet-D4、NIN（双路径混合）、SE-ResNet-D4、EfficientNet-B0、ConvNeXt-Tiny 以及 **CustomViT**。
    *   **CustomViT 设计**：针对 40×40 输入优化，将其划分为 8×8 的 patch（共 25 个 token），嵌入维度 160，包含 6 个 Transformer 块。
    *   **基础模型适配**：选取 ResNet-RS50、CTransPath（病理专用）和 UNI（通用病理基础模型）。由于基础模型要求 224×224 输入，研究采用双三次插值将 40×40 图像放大，并对比了**线性探测（LP）**和**末层微调（FT_last）**两种策略。
    *   **数据增强**：采用几何变换（翻转、转置）结合 5 种颜色空间变换（Gamma 校正、HSV 调整），将训练集扩充至 9.6 倍。

### 3. 实验设计
*   **数据集**：来自 303 名结直肠癌患者的 CD103/CD8 免疫组化染色标本，共包含 185,432 张手工标注的细胞图像。
*   **Benchmark 设置**：
    *   **数据规模实验**：设置 7 个 `FlagLimit` 等级（每类样本数从 256 到 16,384），观察性能随数据量增加的演变。
    *   **鲁棒性测试**：引入高斯模糊扰动，分为“预缩放模糊”（模拟光学失焦）和“后缩放模糊”（模拟数字图像退化）。
*   **对比方法**：横向对比了从传统 MLP 到最新基础模型（如 UNI）在内的 11 种模型方案。

### 4. 资源与算力
*   **硬件环境**：工作站配备单张 **NVIDIA RTX 3060 GPU** 和 64 GB RAM。
*   **训练细节**：
    *   特定任务模型 Batch Size 为 512；基础模型因显存限制，LP 模式 Batch Size 为 13，FT 模式为 5。
    *   训练轮数根据架构不同设定在 30 到 100 epoch 之间，采用 Adam 优化器和早停机制。
    *   文中明确提到了推理时间（ms/patch）作为效率衡量指标。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 种架构、7 种数据梯度、5 种模糊强度以及 2 种模糊模式，实验总数超过百组。
*   **充分性评价**：实验设计非常系统且客观。通过控制变量法分析了数据规模对模型性能的影响，并特别关注了基础模型在小切片任务中的“水土不服”问题。使用了 Macro-F1 等指标平衡类别不均，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **CustomViT 胜出**：针对小切片优化的 CustomViT 在数据充足时（FlagLimit ≥ 4096）准确率最高（F1=0.92），显著优于所有基础模型。
*   **基础模型瓶颈**：基础模型（如 UNI）在极小数据量下表现稳健，但随着数据量增加，其性能提升迅速饱和，且推理成本比 CustomViT 高出一个数量级（25ms vs 1.78ms）。
*   **架构不兼容性**：SE（挤压激励）机制和 EfficientNet 在小切片任务中表现不佳，甚至产生负面影响；ConvNeXt 这种现代 CNN 也不如传统的 ResNet 适配。
*   **鲁棒性误区**：基础模型在面对模糊扰动时并未表现出比轻量级模型更强的鲁棒性，其优势仅体现在洁净数据的绝对准确率上。

### 7. 优点
*   **针对性强**：填补了病理学中 40×40 极小分辨率下深度学习架构选择的理论空白。
*   **效率优先**：证明了在特定医疗场景下，盲目追求“大模型”并非最优解，轻量化定制模型在精度和速度上均有优势。
*   **鲁棒性分析深入**：区分了光学模糊和数字模糊，提供了更贴近临床实际的评估视角。

### 8. 不足与局限
*   **数据集单一**：实验仅基于结直肠癌的免疫组化图像，尚未在其他器官或 H&E 染色图像上验证通用性。
*   **微调策略受限**：受限于算力（RTX 3060），基础模型仅进行了末层微调，未尝试全参数微调，这可能限制了基础模型的上限。
*   **类别覆盖**：虽然样本量大，但主要集中在 16-20 类细胞，对于更罕见的细胞类型或更复杂的组织背景，结论可能需要修正。

（完）
