---
title: Deep EM with Hierarchical Latent Label Modelling for Multi-Site Prostate Lesion Segmentation
title_zh: 基于分层潜在标签建模的深度 EM 用于多中心前列腺病灶分割
authors: "Wen Yan, Yipei Wang, Shiqi Huang, Natasha Thorley, Mark Emberton, Vasilis Stavrinides, Yipeng Hu, Dean Barratt"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14418v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 深度学习用于癌症数据中的前列腺病变分割
tldr: 针对多中心前列腺病灶分割中由于标注协议不一导致的泛化性差问题，本文提出一种分层期望最大化（HierEM）框架。该方法将观测到的标注视为潜在“干净”掩码的噪声表现，通过分层先验建模站点和病例级别的标注偏差，在推断潜在掩码后验分布的同时训练CNN，有效减少了站点特异性偏见，显著提升了模型在未知站点上的泛化性能。
selection_source: fresh_fetch
motivation: 多中心数据集中不同机构的标注风格差异会导致分割网络过拟合局部协议，从而在跨站点应用时表现不佳。
method: 提出HierEM框架，通过分层先验将标注质量分解为全局、站点和病例三个层级，并交替进行潜在掩码推断与网络参数估计。
result: 实验显示该方法在跨站点泛化指标上显著优于现有技术，DSC提升明显，并能输出可解释的站点标注质量评估。
conclusion: 显式建模站点相关的标注变异性是提高医学图像分割模型跨中心鲁棒性和泛化能力的有效途径。
---

## 摘要
标签变异性是前列腺病灶分割面临的主要挑战。在多中心数据集中，标注通常反映了特定中心的勾画方案，导致分割网络过拟合于局部风格，并在推理时对未见中心的泛化能力较差。我们将每个观察到的标注视为底层潜在“干净”病灶掩码的有噪观测，并提出了一种分层期望最大化 (HierEM) 框架，该框架在以下两个步骤之间交替进行：(1) 推断潜在掩码的体素级后验分布；(2) 使用该后验分布作为软目标训练 CNN，并在分层先验下估计特定中心的灵敏度和特异度。这种分层先验将标签质量分解为全局均值以及中心级和病例级的偏差，通过惩罚仅由中心偏差贡献的似然项来减少特定中心的偏差。在三个队列上的实验表明，与现有先进方法相比，所提出的分层 EM 框架增强了跨中心泛化能力。在汇总数据集评估中，各中心的平均 DSC 范围为 29.50% 至 39.69%；在留一中心泛化评估中，范围为 27.91% 至 32.67%，与对比方法相比取得了具有统计学意义的显著改进 (p<0.039)。该方法还生成了可解释的各中心潜在标签质量估计（在特异度 beta 接近 0.99 时，灵敏度 alpha 范围为 31.5% 至 47.3%），支持对跨中心标注变异性的事后分析。这些结果表明，显式建模中心相关的标注可以提高跨中心泛化能力。

## Abstract
Label variability is a major challenge for prostate lesion segmentation. In multi-site datasets, annotations often reflect centre-specific contouring protocols, causing segmentation networks to overfit to local styles and generalise poorly to unseen sites in inference. We treat each observed annotation as a noisy observation of an underlying latent 'clean' lesion mask, and propose a hierarchical expectation-maximisation (HierEM) framework that alternates between: (1) inferring a voxel-wise posterior distribution over the latent mask, and (2) training a CNN using this posterior as a soft target and estimate site-specific sensitivity and specificity under a hierarchical prior. This hierarchical prior decomposes label-quality into a global mean with site- and case-level deviations, reducing site-specific bias by penalising the likelihood term contributed only by site deviations. Experiments on three cohorts demonstrate that the proposed hierarchical EM framework enhances cross-site generalisation compared to state-of-the-art methods. For pooled-dataset evaluation, the per-site mean DSC ranges from 29.50% to 39.69%; for leave-one-site-out generalisation, it ranges from 27.91% to 32.67%, yielding statistically significant improvements over comparison methods (p<0.039). The method also produces interpretable per-site latent label-quality estimates (sensitivity alpha ranges from 31.5% to 47.3% at specificity beta approximates 0.99), supporting post-hoc analyses of cross-site annotation variability. These results indicate that explicitly modelling site-dependent annotation can improve cross-site generalisation.

---

## 论文详细总结（自动生成）

### 论文总结：基于分层潜在标签建模的深度 EM 用于多中心前列腺病灶分割

#### 1. 核心问题与研究动机
*   **核心问题**：前列腺病灶在多参数磁共振成像（mpMRI）中的分割面临严重的**标签变异性（Label Variability）**挑战。不同医疗机构（站点）的标注协议、专家风格及成像设备不同，导致标注存在系统性偏差。
*   **研究动机**：传统的深度学习模型容易过拟合于训练集特定站点的标注风格，导致在未见过的外部站点上泛化性能极差（Dice 分数可能从 60% 跌至 4%）。研究旨在通过建模标注的噪声属性，提升模型在多中心场景下的鲁棒性和跨站点泛化能力。

#### 2. 核心方法论
论文提出了一种**分层期望最大化框架（HierEM）**，将观察到的标注视为潜在“干净”掩码的有噪观测。
*   **核心思想**：利用灵敏度（Sensitivity, $\alpha$）和特异度（Specificity, $\beta$）来量化标注质量，并通过分层先验将其分解为：**全局均值**（共有特征）、**站点效应**（系统性偏差）和**病例效应**（个体难度）。
*   **算法流程（EM 迭代）**：
    *   **E步（推断潜在掩码）**：结合当前分割网络的预测（图像先验）和标注似然项，计算每个体素属于病灶的后验概率，生成“软”共识掩码。
    *   **M步-A（更新网络）**：以 E 步生成的软掩码为目标，通过交叉熵和 Dice 损失训练分割网络（如 UNet）。
    *   **M步-B（更新质量参数）**：利用 L-BFGS 算法优化 MAP 目标函数，估计各站点和病例的灵敏度与特异度参数。
*   **关键技术**：引入逻辑正态（Logistic-Normal）分层先验和 $L_2$ 惩罚项，以稳定在数据稀疏或标注噪声大时的参数估计。

#### 3. 实验设计
*   **数据集**：使用了来自 3 个不同来源的数据集（Site 1: 私有数据 1201 例；Site 2: 公开 PROMIS 队列 391 例；Site 3: 私有数据 1265 例）。
*   **实验场景**：
    1.  **混合池化评估（Split A）**：将所有站点数据混合后按比例拆分训练/测试。
    2.  **留一中心泛化评估（Split B, LOSO）**：在两个站点上训练，在完全未见的第三个站点上测试（最严苛的泛化测试）。
*   **对比方法（Benchmark）**：
    *   标准 **UNet**（基准）。
    *   **Label Bootstrapping**（软自我训练方法）。
    *   **Site-EM**（不带分层结构的独立站点 EM 建模）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到使用了 Adam 优化器，学习率为 $10^{-4}$，每轮 EM 迭代包含 5 个 epoch 的网络更新和 5 步参数优化。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了近 3000 例 mpMRI 影像，样本量在医学影像分割领域属于较大规模。
*   **充分性**：进行了混合训练和留一中心（LOSO）两种模式的对比，并提供了统计学显著性检验（p < 0.05）。
*   **客观性**：通过风险-覆盖曲线（Risk-coverage curves）评估了不确定性，不仅关注分割精度（Dice, HD95），还关注了模型输出的可解释性。

#### 6. 主要结论与发现
*   **泛化性显著提升**：在最挑战的 LOSO 实验中，HierEM 在所有站点上的表现均优于基准 UNet，Dice 分数提升显著（例如 Site 2 从 24.66% 提升至 27.91%）。
*   **减少过拟合**：显式建模站点偏差能有效防止网络学习特定中心的勾画风格，从而提取更本质的病灶特征。
*   **可解释性**：模型成功估计出各站点的标注倾向（如某些站点倾向于“大勾画”导致高灵敏度、低特异度），这为临床数据质量分析提供了工具。

#### 7. 优点与亮点
*   **理论严谨**：将经典的 STAPLE 算法思想融入深度学习 EM 框架，并扩展为分层模型。
*   **即插即用**：该框架与具体的分割网络架构无关，可适配不同的 Backbone。
*   **不确定性校准**：通过建模噪声，模型输出的概率图在不确定性估计上更准确，有助于临床决策。

#### 8. 不足与局限
*   **站点数量有限**：实验仅涉及 3 个站点，对于分层模型而言，站点层级的统计分布估计可能不够稳健（通常站点越多，分层优势越明显）。
*   **体素独立性假设**：模型假设体素间的标注噪声是条件独立的，忽略了空间上下文对标注偏差的影响。
*   **计算复杂度**：相比单次训练，EM 框架需要多次迭代，增加了训练成本。
*   **私有数据限制**：主要数据集为私有，外部研究者难以完全复现其在特定数据集上的结果。

（完）
