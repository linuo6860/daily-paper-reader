---
title: A Contrastive Variational AutoEncoder for NSCLC Survival Prediction with Missing Modalities
title_zh: 一种用于模态缺失下非小细胞肺癌生存预测的对比变分自编码器
authors: "Michele Zanitti, Vanja Miskovic, Francesco Trovò, Alessandra Laura Giulia Pedrocchi, Ming Shen, Yan Kyaw Tun, Arsela Prelaj, Sokol Kosta"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17402v1"
tags: ["query:bio-ca"]
score: 10.0
evidence: 整合转录组学和甲基化组学的癌症生存预测
tldr: 针对非小细胞肺癌（NSCLC）生存预测中多模态数据（病理切片、转录组、DNA甲基化）缺失的问题，本文提出了一种多模态对比变分自编码器（MCVAE）。该模型通过模态特定编码器捕捉不确定性，利用门控融合机制平衡各模态贡献，并结合对比学习与随机掩码策略增强鲁棒性，在处理严重数据缺失时表现优异。
selection_source: fresh_fetch
motivation: 临床多模态数据常面临严重的模态缺失问题，现有预测模型在处理此类不完整数据时缺乏足够的鲁棒性。
method: 提出MCVAE模型，集成变分编码器、门控融合瓶颈、多任务损失函数（生存、重构、对比）及随机模态掩码训练策略。
result: 在TCGA-LUAD和TCGA-LUSC数据集上的实验表明，该方法在疾病特异性生存预测和应对极端缺失场景方面均优于现有SOTA模型。
conclusion: MCVAE有效提升了不完整多模态数据的生存预测性能，同时研究发现多模态集成并非在所有子集组合下都能带来增益。
---

## 摘要
由于个体预后特征的差异，预测非小细胞肺癌（NSCLC）患者的生存结果具有挑战性。该任务可以受益于全切片图像、批量转录组学和 DNA 甲基化的整合，这些数据提供了患者诊断时病情的互补视角。然而，现实世界的临床数据集通常是不完整的，很大一部分患者缺失了整个模态。现有最先进的模型依赖可用数据来创建患者级表示，或使用生成模型来推断缺失模态，但在严重缺失的情况下缺乏鲁棒性。我们提出了一种多模态对比变分自编码器（MCVAE）来解决这个问题：模态特定的变分编码器捕获每个数据源的不确定性，并引入了具有学习门控机制的融合瓶颈，以归一化现有模态的贡献。我们提出了一个多任务目标，结合了生存损失和重建损失来正则化患者表示，以及一个在潜空间中强制执行跨模态对齐的跨模态对比损失。在训练过程中，我们应用随机模态掩码来提高对任意缺失模式的鲁棒性。在 TCGA-LUAD (n=475) 和 TCGA-LUSC (n=446) 数据集上的广泛评估表明，与两种最先进的模型相比，我们的方法在预测疾病特异性生存（DSS）方面具有有效性，并且对严重缺失场景具有鲁棒性。最后，我们通过在所有模态子集上测试我们的模型，对多模态整合提供了一些见解，发现整合并不总是对任务有益。

## Abstract
Predicting survival outcomes for non-small cell lung cancer (NSCLC) patients is challenging due to the different individual prognostic features. This task can benefit from the integration of whole-slide images, bulk transcriptomics, and DNA methylation, which offer complementary views of the patient's condition at diagnosis. However, real-world clinical datasets are often incomplete, with entire modalities missing for a significant fraction of patients. State-of-the-art models rely on available data to create patient-level representations or use generative models to infer missing modalities, but they lack robustness in cases of severe missingness. We propose a Multimodal Contrastive Variational AutoEncoder (MCVAE) to address this issue: modality-specific variational encoders capture the uncertainty in each data source, and a fusion bottleneck with learned gating mechanisms is introduced to normalize the contributions from present modalities. We propose a multi-task objective that combines survival loss and reconstruction loss to regularize patient representations, along with a cross-modal contrastive loss that enforces cross-modal alignment in the latent space. During training, we apply stochastic modality masking to improve the robustness to arbitrary missingness patterns. Extensive evaluations on the TCGA-LUAD (n=475) and TCGA-LUSC (n=446) datasets demonstrate the efficacy of our approach in predicting disease-specific survival (DSS) and its robustness to severe missingness scenarios compared to two state-of-the-art models. Finally, we bring some clarifications on multimodal integration by testing our model on all subsets of modalities, finding that integration is not always beneficial to the task.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MCVAE（Multimodal Contrastive Variational AutoEncoder）** 的深度学习框架，旨在解决非小细胞肺癌（NSCLC）生存预测中普遍存在的**多模态数据缺失**问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在肿瘤学中，整合临床数据、全切片图像（WSI）、转录组学和 DNA 甲基化等多模态数据能显著提升预后预测准确性。
*   **核心问题**：现实临床场景中，由于成本、隐私或采集流程差异，患者往往缺失某些模态的数据（如只有 35.9% 的患者拥有完整数据）。
*   **研究动机**：现有的处理方法（如直接丢弃缺失样本、简单插补或固定表示学习）在面对严重缺失时表现出鲁棒性差、易产生表示偏见（Modality Collapse）等问题。

### 2. 方法论
MCVAE 结合了变分推理、自适应融合和对比学习，核心思想是学习一种**模态无关（Modality-agnostic）**且能捕捉**不确定性**的共享表示。
*   **变分编码（Variational Encoding）**：每个模态拥有独立的变分编码器，将输入映射为高斯分布的均值和方差。方差的大小直接反映了模型对该模态贡献的“信心”或不确定性。
*   **自适应融合与可用性感知门控**：引入可学习的门控参数 $\gamma_k$，结合模态可用性掩码 $a^{(k)}$，对各模态潜变量进行加权归一化融合，确保无论缺失多少模态，融合后的特征尺度保持一致。
*   **多任务学习目标**：
    1.  **生存损失**：基于 Cox 比例风险模型的负对数部分似然损失。
    2.  **重构损失**：通过解码器重建原始模态特征，作为正则化项以保留模态特有信息。
    3.  **KL 散度**：约束潜空间分布，防止后验坍缩。
    4.  **跨模态对比损失（InfoNCE）**：拉近同一患者不同模态间的距离，推开不同患者间的距离，确保潜空间的跨模态对齐。
*   **训练策略**：采用随机模态掩码（Stochastic Modality Masking），在训练中人为随机丢弃非临床模态，增强模型对任意缺失模式的适应力。

### 3. 实验设计
*   **数据集**：使用 TCGA-LUAD（肺腺癌，n=475）和 TCGA-LUSC（肺鳞癌，n=446）。
*   **预测目标**：疾病特异性生存（Disease-Specific Survival, DSS）。
*   **Benchmark**：Harrell's Concordance Index (C-index)。
*   **对比方法**：
    *   **MUSE**：基于图神经网络的表示学习方法（ICLR 2024）。
    *   **M3Care**：基于患者相似性的潜空间插补方法（KDD 2022）。
*   **实验设置**：5 折交叉验证，重复 3 次随机种子（共 15 次实验），并进行统计显著性检验（Friedman & Nemenyi test）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A10 GPU (24GB VRAM)**。
*   **算力说明**：文中指出该算力足以支持使用 UNI 基础模型提取 WSI 特征以及后续所有模型的训练。
*   **训练细节**：使用 AdamW 优化器，最大 150 个 Epoch，包含早停机制（Patience=20）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：对比三种模型在两个数据集上的生存预测性能。
    *   **消融实验**：测试了 8 种不同的模态组合（C, C+T, C+W, C+M 等）。
    *   **敏感性分析**：测试了 6 种不同的训练 Dropout 概率（0.0 到 0.9）。
    *   **鲁棒性测试**：模拟了 5 种不同的测试缺失率（10% 到 90%）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注平均性能，还通过统计检验验证了显著性，并深入探讨了“多模态集成是否总是有效”这一关键科学问题。

### 6. 主要结论与发现
*   **性能领先**：MCVAE 在 LUAD 和 LUSC 队列上均优于 MUSE 和 M3Care，且表现出更低的方差（更稳定）。
*   **极端鲁棒性**：在缺失率高达 90% 的极端情况下，MCVAE 的性能下降最轻微，显示出极强的临床实用潜力。
*   **集成悖论**：研究发现多模态集成并不总是优于单模态。在 LUAD 中，仅使用临床数据的表现有时甚至优于加入 WSI 或组学数据的组合，这暗示了“模态坍缩”或弱信号模态引入噪声的风险。

### 7. 优点
*   **不确定性建模**：利用 VAE 的方差特性处理缺失数据，比确定性模型更符合临床逻辑。
*   **对比学习增强**：通过 InfoNCE 强化了模态间的语义一致性。
*   **训练策略科学**：随机掩码策略有效地模拟了现实中的不完整数据分布。
*   **基础模型应用**：使用了最新的 UNI 基础模型提取病理特征，确保了特征提取的先进性。

### 8. 不足与局限
*   **模态坍缩风险**：实验显示 WSI 模态在某些组合中反而降低了性能，说明模型在处理弱信号模态与强信号模态（如临床数据）的梯度平衡上仍有优化空间。
*   **数据集单一**：仅限于 TCGA 数据库，缺乏外部独立临床队列的验证。
*   **解释性局限**：虽然引入了门控机制，但尚未深入分析门控权重与生物学意义（如特定生物通路）之间的直接联系。

（完）
