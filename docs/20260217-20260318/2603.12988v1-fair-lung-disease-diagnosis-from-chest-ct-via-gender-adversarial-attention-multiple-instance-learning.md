---
title: Fair Lung Disease Diagnosis from Chest CT via Gender-Adversarial Attention Multiple Instance Learning
title_zh: 基于性别对抗注意力多实例学习的胸部 CT 公平肺部疾病诊断
authors: "Aditya Parikh, Aasa Feragen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12988v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 用于 CT 扫描肺癌诊断的深度学习
tldr: 针对CVPR 2026公平疾病诊断挑战赛，本文提出了一种基于性别对抗注意力多实例学习（MIL）的框架，用于胸部CT的多类肺病诊断。该方法利用ConvNeXt提取特征，通过注意力机制识别关键切片，并引入梯度反转层（GRL）抑制性别偏见，结合重采样和集成策略，在解决数据不平衡的同时提升了诊断的公平性与准确性。
selection_source: fresh_fetch
motivation: 旨在解决胸部CT诊断中病理信号稀疏以及因性别和疾病类别分布不均导致的算法偏见问题。
method: 采用基于ConvNeXt的注意力多实例学习框架，并集成梯度反转层进行对抗性性别特征抑制，辅以焦点损失和分层过采样策略。
result: 在验证集上取得了0.685的平均竞争得分，单折最高得分达到0.759，有效平衡了不同性别的诊断性能。
conclusion: 该框架证明了结合对抗学习与多实例学习能有效提升医学影像诊断的公平性，为处理不平衡医疗数据提供了可靠方案。
---

## 摘要
我们提出了一种用于胸部 CT 卷多类肺部疾病诊断的公平感知框架，该框架是为 PHAROS-AIF-MIH 研讨会（CVPR 2026）的公平疾病诊断挑战赛开发的。该挑战赛要求将 CT 扫描分为四类：健康、COVID-19、腺癌和鳞状细胞癌，其性能衡量标准是各性别宏 F1 分数的平均值，明确惩罚性别不平等的预测。我们的方法解决了两个核心困难：数百个切片中稀疏的病理信号，以及疾病类别和性别之间复合的严重人口统计学失衡。我们提出了一种基于 ConvNeXt 骨干网络的注意力多实例学习（MIL）模型，该模型在没有切片级监督的情况下学习识别诊断相关的切片，并辅以梯度反转层（GRL），以对抗性地抑制学习到的扫描表示中具有性别预测性的结构。训练过程结合了带有标签平滑的 Focal Loss、基于（类别、性别）联合层级的分层交叉验证，以及针对代表性最不足的子群体的定向过采样。在推理阶段，所有五折校验点通过软 Logit 投票和水平翻转测试时增强（TTA）进行集成，并利用折外阈值优化以增强鲁棒性。我们的模型实现了 0.685（标准差 0.030）的平均验证竞赛得分，其中表现最好的单折达到了 0.759。所有训练和推理代码均可在 https://github.com/ADE-17/cvpr-fair-chest-ct 公开获取。

## Abstract
We present a fairness-aware framework for multi-class lung disease diagnosis from chest CT volumes, developed for the Fair Disease Diagnosis Challenge at the PHAROS-AIF-MIH Workshop (CVPR 2026). The challenge requires classifying CT scans into four categories -- Healthy, COVID-19, Adenocarcinoma, and Squamous Cell Carcinoma -- with performance measured as the average of per-gender macro F1 scores, explicitly penalizing gender-inequitable predictions. Our approach addresses two core difficulties: the sparse pathological signal across hundreds of slices, and a severe demographic imbalance compounded across disease class and gender. We propose an attention-based Multiple Instance Learning (MIL) model on a ConvNeXt backbone that learns to identify diagnostically relevant slices without slice-level supervision, augmented with a Gradient Reversal Layer (GRL) that adversarially suppresses gender-predictive structure in the learned scan representation. Training incorporates focal loss with label smoothing, stratified cross-validation over joint (class, gender) strata, and targeted oversampling of the most underrepresented subgroup. At inference, all five-fold checkpoints are ensembled with horizontal-flip test-time augmentation via soft logit voting and out-of-the-fold threshold optimization for robustness. Our model achieves a mean validation competition score of 0.685 (std - 0.030), with the best single fold reaching 0.759. All training and inference code is publicly available at https://github.com/ADE-17/cvpr-fair-chest-ct

---

## 论文详细总结（自动生成）

### 论文总结：基于性别对抗注意力多实例学习的胸部 CT 公平肺部疾病诊断

#### 1. 论文的核心问题与整体含义
本文针对医疗 AI 诊断中的**公平性**问题，特别是在胸部 CT 影像分析中可能存在的性别偏见展开研究。研究背景是 CVPR 2026 的“公平疾病诊断挑战赛”，要求对 CT 卷进行四分类（健康、COVID-19、腺癌、鳞状细胞癌）。
其核心挑战在于：
*   **信号稀疏性**：数百张切片中仅有极少数包含病理特征。
*   **人口统计学失衡**：特定类别（如女性鳞状细胞癌）样本极度匮乏。
*   **性别捷径特征**：模型可能通过扫描参数或身体形态等潜在特征识别性别，从而产生偏见预测。

#### 2. 论文提出的方法论
作者提出了一种多组件框架，结合了计算机视觉与公平学习技术：
*   **核心架构（Attention-MIL）**：采用 **ConvNeXt** 作为骨干网络提取单张切片的特征。利用**注意力多实例学习（MIL）**机制，将整个 CT 卷视为一个“包”，通过学习到的权重自动识别对诊断贡献最大的切片，而非简单的平均或最大池化。
*   **性别对抗机制（GRL）**：在扫描表示层（Scan Representation）后接入一个**梯度反转层（GRL）**和性别分类头。在反向传播时，GRL 会反转性别分类的梯度，迫使骨干网络丢弃具有性别预测性的特征，从而实现特征解耦。
*   **训练策略**：
    *   使用 **Focal Loss** 结合**标签平滑（Label Smoothing）**来处理类别不平衡。
    *   实施**分层交叉验证**（基于类别+性别的联合分层）。
    *   采用 **WeightedRandomSampler** 对极少数群体（如女性鳞状细胞癌）进行定向过采样。
*   **推理增强**：结合五折模型集成、水平翻转测试时增强（TTA）以及**折外（Out-of-Fold, OOF）阈值优化**，以提升宏 F1 分数。

#### 3. 实验设计
*   **数据集**：共 889 个 3D 胸部 CT 扫描（734 训练，155 验证）。
*   **分类目标**：腺癌、COVID-19、健康、鳞状细胞癌（SCC）。
*   **评价指标**：男性与女性宏 F1 分数的平均值（$P = \frac{1}{2}(MacroF1_{male} + MacroF1_{female})$）。
*   **对比基准（Benchmark）**：
    *   基线模型：采用平均池化（Mean Pooling）和最大池化（Max Pooling）的切片分类器。
    *   消融实验：逐步加入 Attention-MIL、子群过采样和 GRL 模块进行对比。

#### 4. 资源与算力
*   **硬件**：使用了单块 **NVIDIA RTX A4000 GPU**。
*   **训练细节**：训练 50 个 Epoch，采用梯度累积（Gradient Accumulation）以实现 16 个卷的有效 Batch Size。使用了 AdamW 优化器和余弦退火学习率调度。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 5 折交叉验证，并提供了每折的详细指标（P、M-F1、F-F1 等）。
*   **消融实验**：通过定性分析展示了从基础池化到 Attention-MIL 再到 GRL 的演进过程。
*   **充分性评价**：考虑到数据集规模较小（不足 900 例），5 折交叉验证和 OOF 阈值优化是合理的做法。实验设计较为客观，特别关注了极少数子群体的表现，体现了公平性研究的严谨性。

#### 6. 论文的主要结论与发现
*   **公平性提升**：引入 GRL 后，女性宏 F1 分数（0.691）与男性（0.679）基本持平，甚至略高，证明了对抗学习能有效消除性别偏见。
*   **诊断性能**：模型在验证集上的平均得分为 0.685，单折最高达 0.759。
*   **难点依旧**：鳞状细胞癌（SCC）由于样本极少且与其他病变视觉重叠，仍是性能瓶颈（平均 F1 仅 0.366）。

#### 7. 优点
*   **多层次公平性保障**：不仅在数据层面（过采样）和损失函数层面（Focal Loss）做文章，还从特征表示层面（GRL 对抗学习）主动消除偏见。
*   **架构适配性强**：Attention-MIL 很好地解决了 CT 卷切片数量不一（20 到 800+ 张）且病灶稀疏的问题。
*   **工程落地性高**：推理阶段的 OOF 阈值优化和集成策略显著提升了模型的鲁棒性。

#### 8. 不足与局限
*   **数据极度稀缺**：尽管使用了过采样，但训练集中仅有 18 例女性 SCC 样本，这限制了模型对该特定子群体的泛化能力。
*   **计算开销**：MIL 架构处理 3D 卷时对显存要求较高，文中需将切片数限制在 32 张，可能丢失部分细微病灶信息。
*   **外部验证缺失**：目前仅在挑战赛提供的数据集上进行了验证，尚未在其他独立的多中心数据集上测试其公平性迁移能力。

（完）
