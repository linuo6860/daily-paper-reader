---
title: "SCALE:Scalable Conditional Atlas-Level Endpoint transport for virtual cell perturbation prediction"
title_zh: SCALE：用于虚拟细胞扰动预测的可扩展条件图谱级终点传输
authors: "Shuizhou Chen, Lang Yu, Kedu Jin, Songming Zhang, Hao Wu, Wenxuan Huang, Sheng Xu, Quan Qian, Qin Chen, Lei Bai, Siqi Sun, Zhangyang Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17380v1"
tags: ["query:rna-int"]
score: 9.0
evidence: 基于单细胞测量的虚拟细胞扰动预测基础模型
tldr: SCALE是一个针对虚拟细胞扰动预测的大规模基础模型，旨在解决现有模型在训练效率、建模稳定性和生物学评估方面的局限。它基于BioNeMo框架提升了扩展性，采用条件传输与LLaMA编码器结合的架构，并在Tahoe-100M基准上通过生物学指标验证了其优越性，显著提升了预测精度和计算效率。
selection_source: fresh_fetch
motivation: 现有的虚拟细胞扰动预测面临训练推理效率低、高维稀疏空间建模不稳定以及评估指标缺乏生物学忠实度的问题。
method: 提出SCALE模型，结合BioNeMo分布式框架、基于LLaMA的细胞编码器以及面向端点的条件传输流架构。
result: "在Tahoe-100M基准上，预训练速度提升12.51倍，PDCorr和DE Overlap指标分别比现有SOTA提升了12.02%和10.66%。"
conclusion: 推进虚拟细胞研究需要协同设计可扩展的基础设施、稳定的传输建模以及符合生物学意义的评估体系。
---

## 摘要
虚拟细胞模型旨在通过单细胞测量数据预测细胞对遗传、化学或细胞因子扰动的反应，从而实现干法实验（in silico experimentation）。然而在实践中，大规模扰动预测仍受限于三个相互关联的瓶颈：低效的训练和推理流水线、高维稀疏表达空间中不稳定的建模，以及过度强调重建准确性而低估生物学保真度的评估协议。在这项工作中，我们提出了一个专门用于虚拟细胞扰动预测的大规模基础模型 SCALE，共同解决了上述局限性。首先，我们构建了一个基于 BioNeMo 的训练和推理框架，显著提高了数据吞吐量、分布式可扩展性和部署效率，在相同系统设置下，其预训练速度比之前的 SOTA 流水线提升了 12.51 倍，推理速度提升了 1.29 倍。其次，我们将扰动预测表述为条件传输（conditional transport），并利用一种集合感知流架构（set-aware flow architecture）来实现，该架构将基于 LLaMA 的细胞编码与面向终点的监督相结合。这种设计带来了更稳定的训练和更强的扰动效应恢复能力。第三，我们在 Tahoe-100M 上使用严格的细胞级协议对模型进行评估，该协议以具有生物学意义的指标为中心，而非仅仅关注重建。在该基准测试中，我们的模型相比 STATE 将 PDCorr 提高了 12.02%，DE Overlap 提高了 10.66%。总之，这些结果表明，推进虚拟细胞不仅需要更好的生成目标，还需要可扩展基础设施、稳定传输建模和生物学忠实评估的协同设计。

## Abstract
Virtual cell models aim to enable in silico experimentation by predicting how cells respond to genetic, chemical, or cytokine perturbations from single-cell measurements. In practice, however, large-scale perturbation prediction remains constrained by three coupled bottlenecks: inefficient training and inference pipelines, unstable modeling in high-dimensional sparse expression space, and evaluation protocols that overemphasize reconstruction-like accuracy while underestimating biological fidelity. In this work we present a specialized large-scale foundation model SCALE for virtual cell perturbation prediction that addresses the above limitations jointly. First, we build a BioNeMo-based training and inference framework that substantially improves data throughput, distributed scalability, and deployment efficiency, yielding 12.51* speedup on pretrain and 1.29* on inference over the prior SOTA pipeline under matched system settings. Second, we formulate perturbation prediction as conditional transport and implement it with a set-aware flow architecture that couples LLaMA-based cellular encoding with endpoint-oriented supervision. This design yields more stable training and stronger recovery of perturbation effects. Third, we evaluate the model on Tahoe-100M using a rigorous cell-level protocol centered on biologically meaningful metrics rather than reconstruction alone. On this benchmark, our model improves PDCorr by 12.02% and DE Overlap by 10.66% over STATE. Together, these results suggest that advancing virtual cells requires not only better generative objectives, but also the co-design of scalable infrastructure, stable transport modeling, and biologically faithful evaluation.

---

## 论文详细总结（自动生成）

### 论文总结：SCALE —— 用于虚拟细胞扰动预测的可扩展条件图谱级终点传输

#### 1. 核心问题与整体含义（研究动机和背景）
虚拟细胞模型的核心目标是实现“干法实验”（in silico），即通过计算模型预测细胞在受到遗传敲除、化学药物或细胞因子干预后的状态变化。
**研究动机：** 尽管已有模型取得进展，但仍面临三大瓶颈：
*   **效率低下：** 现有的训练和推理流水线难以处理亿级规模的单细胞图谱数据。
*   **建模不稳定：** 单细胞数据具有高维、稀疏和异质性，直接在表达空间建模容易导致训练不稳定。
*   **评估偏差：** 现有评估协议过度关注均方误差（MSE）等重建指标，而忽视了生物学上的真实性（如差异表达基因的准确性）。

#### 2. 方法论：核心思想与关键技术
SCALE 提出了一个端到端的框架，将扰动预测重新定义为**条件传输（Conditional Transport）**问题。
*   **层次化集合感知编码器（Hierarchical Set-Aware Encoder）：**
    *   **基因级编码：** 采用类 LLaMA 的注意力机制处理细胞内的基因依赖关系。
    *   **细胞级聚合：** 利用 DeepSets 结构捕捉细胞群体（Set）的分布特征，确保模型对细胞顺序具有置换不变性。
*   **条件传输流架构（Conditional Flow Architecture）：**
    *   模型不假设细胞间的一一对应，而是学习从对照组（Control）群体到扰动组（Perturbed）群体的分布演变。
    *   **JiT 参数化策略：** 采用“端点预测（x-pred）”和“端点损失（x-loss）”的组合。相比于预测中间速度场，直接预测扰动后的最终状态（Endpoint）在单细胞场景下更稳定且更符合观测数据。
*   **损失函数：** 结合了均方误差（MSE）和最大均值差异（MMD），旨在同时保证点对点的重建精度和群体水平的分布对齐。

#### 3. 实验设计
*   **数据集：**
    *   **Tahoe-100M：** 包含超过 1 亿个单细胞、1100 多种化学处理的大规模药物扰动数据集。
    *   **PBMC：** 涉及 90 种细胞因子扰动，侧重于免疫信号传导。
    *   **Replogle-Nadig：** 包含 2024 种遗传扰动（CRISPRi）的大规模遗传学数据集。
*   **基准（Benchmark）与对比方法：**
    *   对比了 **STATE**（之前的 SOTA）、**PerturbDiff**（扩散模型）、**CPA**、**GEARS**、**CellFlow** 以及线性基准（Linear）。
    *   评估指标侧重于生物学意义：**PDCorr**（扰动相关性）、**DE Overlap**（差异表达基因重叠度）、**LFC Spearman**（对数倍数变化相关性）。

#### 4. 资源与算力
*   **基础设施：** 论文构建了基于 **NVIDIA BioNeMo** 的训练和推理框架，利用分布式执行和高效的 I/O 处理（如 LMDB 分片存储）。
*   **算力表现：** 论文明确提到在匹配的系统设置下，预训练速度实现了 **12.51 倍** 的加速，推理速度提升了 **1.29 倍**。
*   **具体硬件：** 文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但强调了其在处理 Tahoe-100M 这种超大规模数据集时的扩展能力。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在三个不同领域（化学、免疫、遗传）的大型数据集上进行了验证，实验覆盖面广。
*   **消融实验：** 针对条件聚合方式（均值池化 vs. 种子注意力）、JiT 训练变体（x-pred vs. v-pred）、以及先验分布设计（高斯混合 vs. 掩码）进行了详细的对比。
*   **扩展性研究：** 比较了 184M 和 280M 参数规模的模型表现。
*   **客观性：** 实验采用了严格的“细胞级评估协议”（Cell-Eval），并指出了模型在 MSE 指标上可能不如基准但在生物学指标上更优的现象，分析较为客观。

#### 6. 主要结论与发现
*   **性能超越：** 在 Tahoe-100M 上，SCALE 将 PDCorr 提升了 12.02%，DE Overlap 提升了 10.66%。
*   **指标解耦：** 发现 MSE/MAE 等重建指标与生物学准确性（DE 基因恢复）之间存在解耦。优化 MSE 往往会导致模型陷入“平均效应陷阱”，而 SCALE 通过端点传输更好地捕捉了异质性响应。
*   **参数饱和：** 扩展性研究显示，在当前数据规模下，模型性能在 184M 参数左右趋于饱和，进一步增加参数（至 280M）带来的生物学增益有限。

#### 7. 优点（亮点）
*   **系统性优化：** 不仅改进了算法，还通过 BioNeMo 解决了大规模单细胞数据的工程落地问题。
*   **建模视角独特：** 放弃了难以观测的连续轨迹重建，转而采用更稳健的端点对齐传输建模。
*   **生物学忠实度：** 强调并证明了在虚拟细胞领域，生物学指标（如 DE 基因）比纯数学重建指标更重要。

#### 8. 不足与局限
*   **评估协议敏感性：** 作者坦承 Cell-Eval 协议对实现细节（如预处理、序列化）非常敏感，不同代码库的复现结果可能存在波动。
*   **捷径学习风险：** 在对照组和扰动组状态非常接近时，流匹配模型可能学习到简单的局部插值“捷径”，而非真正的生物学因果规律。
*   **跨研究对比困难：** 由于各论文预处理和划分策略不一，与早期方法的数值对比可能存在协议不完全对齐的风险。

（完）
