---
title: Diffusion-Based Feature Denoising and Using NNMF for Robust Brain Tumor Classification
title_zh: 基于扩散的特征去噪及利用 NNMF 进行鲁棒性脑肿瘤分类
authors: "Hiba Adil Al-kharsan, Róbert Rajkó"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13182v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 用于脑肿瘤分类的深度学习和非负矩阵分解
tldr: 本研究针对脑肿瘤MRI分类中深度学习模型易受对抗攻击的问题，提出了一种结合非负矩阵分解（NNMF）、轻量级卷积神经网络（CNN）和基于扩散的特征净化模块的鲁棒分类框架。通过NNMF提取可解释特征并利用统计指标筛选，再结合扩散模型在特征空间进行去噪净化，有效提升了模型在面对对抗扰动时的可靠性与安全性。
selection_source: fresh_fetch
motivation: 深度学习模型在脑肿瘤分类中虽表现优异，但其对对抗性扰动的敏感性限制了其在敏感医疗诊断中的实际应用。
method: 该框架利用NNMF提取可解释特征，通过统计学方法筛选关键成分，并引入基于扩散模型的特征空间净化模块来抵御对抗攻击。
result: 实验结果显示，该方法在保持竞争性分类准确率的同时，显著提升了在AutoAttack等强力攻击下的鲁棒性。
conclusion: 将可解释的NNMF特征与扩散防御技术相结合，为对抗环境下的医疗图像分类提供了一种有效且可靠的解决方案。
---

## 摘要
基于磁共振成像（MRI）的脑肿瘤分类在计算机辅助诊断系统中发挥着重要作用。近年来，深度学习模型已实现了较高的分类准确率。然而，它们对对抗性扰动的敏感性已成为医疗应用中一个重要的可靠性问题。本研究提出了一种鲁棒的脑肿瘤分类框架，该框架结合了非负矩阵分解（NNMF 或 NMF）、轻量级卷积神经网络（CNN）以及基于扩散的特征纯化。首先，对 MRI 图像进行预处理并转换为非负数据矩阵，从中提取紧凑且具有可解释性的 NNMF 特征表示。使用包括 AUC、Cohen's d 和 p 值在内的统计指标对最具判别性的成分进行排序和选择。然后，直接在选定的特征组上训练轻量级 CNN 分类器。为了提高对抗鲁棒性，引入了基于扩散的特征空间纯化模块。在分类之前，采用前向加噪方法及随后的学习型去噪网络。系统性能通过在 AutoAttack 生成的强力对抗攻击下的干净准确率和鲁棒准确率进行评估。实验结果表明，所提出的框架在实现具有竞争力的分类性能的同时，显著增强了针对对抗性扰动的鲁棒性。研究结果表明，将基于 NNMF 的可解释表示与轻量级深度方法及基于扩散的防御技术相结合，为对抗环境下的医学图像分类提供了一种有效且可靠的解决方案。

## Abstract
Brain tumor classification from magnetic resonance imaging, which is also known as MRI, plays a sensitive role in computer-assisted diagnosis systems. In recent years, deep learning models have achieved high classification accuracy. However, their sensitivity to adversarial perturbations has become an important reliability concern in medical applications. This study suggests a robust brain tumor classification framework that combines Non-Negative Matrix Factorization (NNMF or NMF), lightweight convolutional neural networks (CNNs), and diffusion-based feature purification. Initially, MRI images are preprocessed and converted into a non-negative data matrix, from which compact and interpretable NNMF feature representations are extracted. Statistical metrics, including AUC, Cohen's d, and p-values, are used to rank and choose the most discriminative components. Then, a lightweight CNN classifier is trained directly on the selected feature groups. To improve adversarial robustness, a diffusion-based feature-space purification module is introduced. A forward noise method followed by a learned denoiser network is used before classification. System performance is estimated using both clean accuracy and robust accuracy under powerful adversarial attacks created by AutoAttack. The experimental results show that the proposed framework achieves competitive classification performance while significantly enhancing robustness against adversarial perturbations.The findings presuppose that combining interpretable NNMF-based representations with a lightweight deep approach and diffusion-based defense technique supplies an effective and reliable solution for medical image classification under adversarial conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diffusion-Based Feature Denoising and Using NNMF for Robust Brain Tumor Classification》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决**深度学习模型在脑肿瘤 MRI 分类任务中的脆弱性**。虽然卷积神经网络（CNN）在医疗影像识别上表现优异，但极易受到**对抗性攻击（Adversarial Attacks）**的影响——即通过在图像中加入微小且不可见的扰动，就能导致模型误诊。在安全敏感的医疗领域，这种不稳定性是致命的。
研究动机是构建一个既具有**可解释性**又具备**强鲁棒性**的分类框架，通过结合传统矩阵分解技术与现代扩散模型（Diffusion Models）来净化受损特征，确保诊断的可靠性。

### 2. 方法论：核心思想与技术细节
该框架由四个关键阶段组成：
*   **特征提取（NNMF）**：将预处理后的 MRI 图像转换为非负矩阵，利用**非负矩阵分解（NNMF）**将其分解为基矩阵 $W$ 和系数矩阵 $H$。相比 PCA，NNMF 的非负约束能产生更符合解剖学意义的“局部组合”表示。
*   **统计筛选**：并非所有 NNMF 成分都对分类有用。研究利用 **AUC（曲线下面积）**、**Cohen’s d（效应量）**和 **Welch’s t-test（显著性检验）** 对特征进行排序，选出最具判别性的前 $M$ 个特征。
*   **轻量级 CNN 分类**：在选定的低维特征空间（而非原始高维图像）上训练一个轻量级的 CNN 模型。
*   **扩散净化防御（核心防御机制）**：
    *   **前向过程**：在特征空间引入受控的随机噪声。
    *   **去噪网络（Denoiser）**：训练一个回归神经网络，学习将受扰动的特征 $x_t$ 还原为原始干净特征 $x_0$。
    *   **推理阶段**：当面临对抗攻击时，系统先通过扩散-去噪过程“洗掉”攻击载荷，再进行分类。

### 3. 实验设计
*   **数据集**：使用 Kaggle 上的 Brain Tumor Image Dataset，包含约 2,200 张 MRI 图像（分为 Normal 和 Tumor 两类）。
*   **划分比例**：训练集 70%，验证集 20%，测试集 10%。
*   **Benchmark（基准攻击）**：采用 **AutoAttack (AA+)**，这是目前公认最严苛的对抗鲁棒性评估工具，具体使用了 APGD-CE 和 Square 两种攻击算法。
*   **对比对象**：对比了“干净模型（无防御）”与“防御模型（带扩散净化）”在干净样本和对抗样本下的表现。

### 4. 资源与算力
*   **硬件环境**：工作站配备 Intel Core i7 CPU，16GB RAM，以及 NVIDIA RTX 系列 GPU（支持 CUDA 加速）。
*   **算力消耗**：
    *   **NNMF 特征提取**：GPU 耗时约 9.11 秒。
    *   **CNN 训练**：GPU 耗时约 17.22 秒。
    *   **扩散去噪器训练**：GPU 耗时约 9.24 秒。
    *   **AutoAttack 评估（防御状态下）**：GPU 耗时约 70.30 秒。
    *   **总运行时间**：GPU 模式下约为 116.60 秒，CPU 模式下约为 201.47 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 15 个主要 NNMF 特征进行了深入的统计分析（包括分布图、热力图、相关性分析等）。
*   **评估指标**：不仅使用了准确率（Accuracy），还引入了 **Brier Score**、**Log-Loss**、**MCC（马修斯相关系数）**、**F1-score** 和 **ROC-AUC** 等多维度指标。
*   **充分性评价**：实验设计较为客观，通过 AutoAttack 这一强力基准验证了防御的有效性。然而，实验主要集中在二分类任务上，且数据集规模（2200张）在深度学习领域属于中等偏小。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在 $L_\infty, \epsilon=0.10$ 的强力攻击下，基准模型的鲁棒准确率几乎降至 **0%**（MCC 为 -0.99），而经过扩散净化的模型鲁棒准确率保持在 **59.53%**。
*   **性能权衡**：防御机制会对干净样本的准确率产生轻微负面影响（从 86.05% 降至 85.12%），但在对抗环境下这种牺牲是值得的。
*   **可解释性验证**：通过统计分析证明，NNMF 提取的特征确实对应于大脑的解剖结构（如颅骨边界、组织分布），增强了医疗诊断的可信度。

### 7. 优点（亮点）
*   **特征空间防御**：不同于传统的像素级防御，在低维 NNMF 特征空间进行扩散去噪，计算开销更小且针对性更强。
*   **统计与深度学习结合**：利用严谨的统计指标（Cohen's d 等）进行特征筛选，避免了黑盒模型的盲目性。
*   **轻量化**：模型结构简单，推理速度快，适合集成到实时辅助诊断系统中。

### 8. 不足与局限
*   **数据泄漏风险**：作者在文中坦诚，由于缺乏患者 ID 信息，可能存在“切片级泄漏”（即同一患者的不同切片分到了训练集和测试集），这可能导致干净准确率被高估。
*   **任务复杂度有限**：目前仅针对“有瘤/无瘤”的二分类，未涉及多分类（如不同类型的肿瘤）或分割任务。
*   **防御上限**：虽然鲁棒性大幅提升，但在面对最强攻击时，约 60% 的准确率距离临床完全可靠仍有提升空间。

（完）
