---
title: Class Visualizations and Activation Atlases for Enhancing Interpretability in Deep Learning-Based Computational Pathology
title_zh: 类别可视化与激活图谱用于增强深度学习计算病理学的可解释性
authors: "Marco Gustav, Fabian Wolf, Christina Glasner, Nic G. Reitsam, Stefan Schulz, Kira Aschenbroich, Bruno Märkl, Sebastian Foersch, Jakob Nikolas Kather"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07170v2"
tags: ["query:bio-ca"]
score: 9.0
evidence: 深度学习用于计算病理学中的分子和临床生物标志物预测
tldr: 本研究针对计算病理学中 Transformer 模型可解释性不足的问题，系统评估了类别可视化（CVs）和激活图谱（AAs）在病理基础模型中的应用。研究涵盖了组织分类和多器官癌症分类任务，通过四位病理学家的标注实验，量化了生成图像的可解释性。结果表明，这些方法能有效展示模型学习到的形态学特征，但在细分癌症子类时存在局限。该研究为理解深度学习模型在病理学中的内部表征提供了新视角和评估框架。
selection_source: fresh_fetch
motivation: 旨在解决计算病理学中 Transformer 模型日益复杂但缺乏直观可解释性的问题，探索特征可视化在病理诊断中的适用性。
method: 开发了一个可视化框架，利用类别可视化和激活图谱对基础模型进行多粒度任务评估，并结合病理学家的专业标注进行定量分析。
result: 可视化方法在区分形态差异明显的组织时效果显著，但在重叠的癌症子类中表现受限，且激活图谱的组织结构呈现出明显的层级依赖性。
conclusion: 概念级特征可视化揭示了病理模型内部的形态学流形，为专家审查和理解深度学习模型的表征逻辑提供了有效的工具和框架。
---

## 摘要
基于 Transformer 的模型在计算病理学中的快速应用，使得从 H&E 全切片图像中预测分子和临床生物标志物成为可能，然而其可解释性的发展尚未跟上模型复杂度的提升。虽然基于归因和生成的方法较为常见，但诸如类别可视化（CVs）和激活图谱（AAs）等特征可视化方法尚未针对这些模型进行系统评估。我们开发了一个可视化框架，并在标签粒度不断增加的组织和多器官癌症分类任务中，评估了基于 Transformer 的基础模型的 CVs 和 AAs。四名病理学家对真实和生成的图像进行了标注，以量化观察者间的一致性，并辅以归因和相似性指标。CVs 保持了形态差异显著组织的识别度，但在重叠的癌症亚类中表现出可分离性下降。在组织分类中，一致性从 Fleiss k = 0.75（扫描图）下降到 k = 0.31（CVs），在癌症亚类任务中也呈现类似趋势。AAs 揭示了依赖于层的组织结构：粗粒度的组织级概念形成了连贯区域，而更细分的亚类则表现出分散和重叠。组织分类的一致性为中等（k = 0.58），粗粒度癌症分组的一致性较高（k = 0.82），而亚类水平的一致性较低（k = 0.11）。图谱的可分离性与专家在真实图像上的一致性密切相关，这表明表征的模糊性反映了内在的病理复杂性。在低复杂度设置下，基于归因的指标接近专家的变异性，而感知和分布指标显示出有限的一致性。总体而言，概念级特征可视化揭示了基于 Transformer 的病理模型中结构化的形态流形，并为跨标签粒度的学习表征提供了以专家为中心的探究框架。

## Abstract
The rapid adoption of transformer-based models in computational pathology has enabled prediction of molecular and clinical biomarkers from H&E whole-slide images, yet interpretability has not kept pace with model complexity. While attribution- and generative-based methods are common, feature visualization approaches such as class visualizations (CVs) and activation atlases (AAs) have not been systematically evaluated for these models. We developed a visualization framework and assessed CVs and AAs for a transformer-based foundation model across tissue and multi-organ cancer classification tasks with increasing label granularity. Four pathologists annotated real and generated images to quantify inter-observer agreement, complemented by attribution and similarity metrics. CVs preserved recognizability for morphologically distinct tissues but showed reduced separability for overlapping cancer subclasses. In tissue classification, agreement decreased from Fleiss k = 0.75 (scans) to k = 0.31 (CVs), with similar trends in cancer subclass tasks. AAs revealed layer-dependent organization: coarse tissue-level concepts formed coherent regions, whereas finer subclasses exhibited dispersion and overlap. Agreement was moderate for tissue classification (k = 0.58), high for coarse cancer groupings (k = 0.82), and low at subclass level (k = 0.11). Atlas separability closely tracked expert agreement on real images, indicating that representational ambiguity reflects intrinsic pathological complexity. Attribution-based metrics approximated expert variability in low-complexity settings, whereas perceptual and distributional metrics showed limited alignment. Overall, concept-level feature visualization reveals structured morphological manifolds in transformer-based pathology models and provides a framework for expert-centered interrogation of learned representations across label granularities.

---

## 论文详细总结（自动生成）

这是一份关于论文《Class Visualizations and Activation Atlases for Enhancing Interpretability in Deep Learning-Based Computational Pathology》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在计算病理学中，基于 Transformer 的基础模型（如 UNI）在预测分子生物标志物和临床预后方面表现卓越。然而，这些模型通常被视为“黑盒”，现有的可解释 AI（XAI）方法（如热力图或归因法）大多针对单张图像的特定预测，无法揭示模型内部如何组织和理解全局的形态学概念。
*   **核心问题**：模型内部的特征空间是否形成了具有病理学意义的结构？这些表征在不同任务粒度下是否稳定？
*   **整体含义**：本研究旨在通过“概念级”可视化技术，系统地探究病理基础模型内部的形态学流形，验证模型学习到的特征是否与人类病理学家的医学逻辑相符。

### 2. 方法论
*   **核心思想**：利用特征可视化技术将高维的数学表征转化为病理学家可理解的合成图像。
*   **关键技术细节**：
    *   **类别可视化 (Class Visualizations, CVs)**：固定模型参数，通过梯度上升法优化输入的随机噪声图像，使其在模型最后一层产生的特定类别 Logit 值最大化，从而生成该类别的“原型图像”。
    *   **激活图谱 (Activation Atlases, AAs)**：
        1.  **激活捕获**：提取模型中间层（如 UNI 的第 14 层）的特征向量。
        2.  **降维与聚合**：使用 t-SNE 将高维激活向量投影到 2D 空间，并划分为网格，对每个网格内的向量取平均值。
        3.  **特征反演 (Feature Inversion)**：通过迭代优化，生成一张合成图像，使其在模型中的激活值与网格平均向量的欧氏距离最小。
    *   **评估指标**：引入了归因分数（Attribution）、感知相似度指标（LPIPS, DreamSim）和马氏距离（Mahalanobis Distance）来量化生成图像与真实图像的匹配度。

### 3. 实验设计
*   **数据集**：
    *   **NCT-CRC-HE-100K**：包含 9 类结直肠组织（如肿瘤、间质、淋巴、脂肪等）。
    *   **TCGA (The Cancer Genome Atlas)**：涵盖 11 种实体瘤，并设计了不同粒度的任务（如区分 5 类、8 类、11 类癌症，以及汇总的腺癌类别）。
*   **Benchmark (基准)**：以 4 位病理学家对真实扫描图像（Scans）的标注一致性（Fleiss’ κ）作为性能上限。
*   **对比方法**：对比了生成的 CVs、AAs 与真实图像在专家标注下的一致性，并评估了多种自动化度量指标作为专家判断替代方案的有效性。

### 4. 资源与算力
*   **算力支持**：论文提到使用了德累斯顿工业大学（TU Dresden）的 ZIH 高性能计算设施进行大规模计算。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及模型训练和图像生成的总时长。但提到 UNI 基础模型权重是冻结的，仅训练了顶部的线性分类层。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了从简单的组织分类到复杂的多器官癌症亚类分类。对 UNI 模型的所有 24 层都进行了激活分析（见补充材料），并最终选定中间层进行深入研究。
*   **充分性与客观性**：
    *   实验设计了多粒度的标签体系，能够观察模型在不同难度下的表现。
    *   引入 4 位专家进行独立标注，并使用多种统计学指标（Fleiss’ κ, Cohen’s κ, Krippendorff’s α）进行定量分析，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **形态学对应性**：模型成功捕捉到了关键病理特征，如脂肪组织的空泡结构、淋巴细胞的密集核形态等。
*   **层级演化**：随着网络深度增加，可视化图像从简单的纹理逐渐演变为复杂的细胞和组织结构，反映了特征的逐层抽象。
*   **粒度敏感性**：在粗粒度任务中，模型表征清晰且专家一致性高；但在细粒度任务（如区分不同器官的腺癌）中，模型表征出现重叠，这与病理学家在临床诊断中遇到的模糊性高度一致。
*   **指标效用**：基于归因的指标能较好地模拟专家的一致性，而传统的马氏距离在处理复杂的病理特征空间时表现较差。

### 7. 优点
*   **创新性**：首次将激活图谱（AA）大规模应用于基于 Transformer 的病理学基础模型。
*   **以人为本**：通过专家标注实验，将 AI 的内部逻辑与人类医学知识进行了直接对比。
*   **系统性**：不仅展示了“模型看到了什么”，还揭示了模型在不同任务难度下的“困惑度”。

### 8. 不足与局限
*   **感知指标偏差**：所使用的 LPIPS 和 DreamSim 等指标是在自然图像（ImageNet）上预训练的，可能无法完全捕捉病理图像中细微的细胞级差异。
*   **计算开销**：特征反演过程涉及大量的迭代优化，对于大规模临床应用而言，生成速度可能是一个瓶颈。
*   **空间上下文缺失**：目前的分析主要基于 Patch（图像块），尚未能完全解释全切片图像（WSI）中长距离的空间组织结构。

（完）
