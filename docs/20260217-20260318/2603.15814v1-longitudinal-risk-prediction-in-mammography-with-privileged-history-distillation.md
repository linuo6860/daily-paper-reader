---
title: Longitudinal Risk Prediction in Mammography with Privileged History Distillation
title_zh: 基于特权历史蒸馏的乳腺 X 线摄影纵向风险预测
authors: "Banafsheh Karimian, Alexis Guichemerre, Soufiane Belharbi, Natacha Gillet, Luke McCaffrey, Mohammadhadi Shateri, Eric Granger"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15814v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 利用纵向乳腺摄影和机器学习进行乳腺癌风险预测
tldr: 针对乳腺癌风险预测中历史影像缺失的问题，本文提出了一种特权历史蒸馏（PHD）方法。该方法在训练阶段利用完整的纵向历史影像作为特权信息，通过多教师蒸馏机制将预后价值传递给仅需当前影像的学生模型。在CSAW-CC数据集上的实验表明，该方法在推理时无需历史影像，即可达到与全历史模型相当的长周期预测性能，显著提升了临床适用性。
selection_source: fresh_fetch
motivation: 临床实践中纵向乳腺影像历史常因漏检或存档限制而缺失，导致依赖历史数据的风险预测模型性能下降且难以普及。
method: 提出一种特权多教师蒸馏方案，利用训练时的完整历史影像训练特定预测周期的教师模型，并将其知识蒸馏至仅输入当前影像的学生模型中。
result: 在CSAW-CC数据集上，该方法在仅使用当前影像的情况下，长周期预测的AUC表现显著优于无历史模型，且与全历史模型相当。
conclusion: 特权历史蒸馏有效地解决了推理时历史影像缺失的问题，在保持高预测精度的同时增强了纵向风险模型的实际临床应用价值。
---

## 摘要
乳腺癌仍然是全球癌症相关死亡的主要原因。纵向乳腺 X 线摄影风险预测模型基于先前的筛查检查，提高了多年乳腺癌风险预测的准确性。然而，在现实临床实践中，由于漏诊、首次检查、异质性采集计划或存档限制，纵向历史记录通常是不完整、不规则或不可用的。缺乏先前的检查会降低纵向风险模型的性能，并限制其在实际中的应用。虽然在训练期间可以获得大量的纵向历史记录，但在测试时通常缺乏先前的检查。在本文中，我们解决了推理时的历史缺失问题，并提出了一种纵向风险预测方法，该方法在训练期间将乳腺 X 线摄影历史作为特权信息，并将其预后价值蒸馏到一个在推理时仅需要当前检查的学生模型中。核心思想是一种具有特定预测跨度教师的特权多教师蒸馏方案：每个教师都在完整的纵向历史上进行训练，以专注于一个预测跨度，而学生模型仅接收从当前检查中导出的重构历史。这使得学生模型能够继承与预测跨度相关的纵向风险线索，而无需在部署时提供先前的筛查检查。我们提出的新型特权历史蒸馏（PHD）方法在具有多年癌症结局的大型纵向乳腺 X 线摄影数据集 CSAW-CC 上进行了验证，并将全历史和无历史基准与其蒸馏后的对应模型进行了比较。通过使用跨预测跨度的时间依赖性 AUC，我们的特权历史蒸馏方法显著提高了长跨度预测相对于无历史模型的性能，并且与全历史模型的性能相当，同时在推理时仅使用当前检查。

## Abstract
Breast cancer remains a leading cause of cancer-related mortality worldwide. Longitudinal mammography risk prediction models improve multi-year breast cancer risk prediction based on prior screening exams. However, in real-world clinical practice, longitudinal histories are often incomplete, irregular, or unavailable due to missed screenings, first-time examinations, heterogeneous acquisition schedules, or archival constraints. The absence of prior exams degrades the performance of longitudinal risk models and limits their practical applicability. While substantial longitudinal history is available during training, prior exams are commonly absent at test time. In this paper, we address missing history at inference time and propose a longitudinal risk prediction method that uses mammography history as privileged information during training and distills its prognostic value into a student model that only requires the current exam at inference time. The key idea is a privileged multi-teacher distillation scheme with horizon-specific teachers: each teacher is trained on the full longitudinal history to specialize in one prediction horizon, while the student receives only a reconstructed history derived from the current exam. This allows the student to inherit horizon-dependent longitudinal risk cues without requiring prior screening exams at deployment. Our new Privileged History Distillation (PHD) method is validated on a large longitudinal mammography dataset with multi-year cancer outcomes, CSAW-CC, comparing full-history and no-history baselines to their distilled counterparts. Using time-dependent AUC across horizons, our privileged history distillation method markedly improves the performance of long-horizon prediction over no-history models and is comparable to that of full-history models, while using only the current exam at inference time.

---

## 论文详细总结（自动生成）

### 论文总结：基于特权历史蒸馏的乳腺 X 线摄影纵向风险预测

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：纵向乳腺癌风险预测模型（利用患者多次历史筛查影像）虽然比单次影像模型更准确，但在临床实践中面临**历史数据缺失**的挑战（如首次检查、跨院就诊、档案丢失等）。
*   **研究动机**：现有的纵向模型（如 LoMaR, VMRA）在推理时高度依赖历史影像，一旦缺失，性能会大幅下降。
*   **整体含义**：本文旨在开发一种方法，在**训练阶段**利用完整的历史信息（作为“特权信息”），通过**知识蒸馏**将这些时间演化特征传递给学生模型，使得学生模型在**推理阶段**仅凭当前单次影像即可实现接近全历史模型的预测精度。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出**特权历史蒸馏（PHD）**框架。利用“训练时可见、测试时不可见”的历史数据作为特权信息，通过多教师机制引导学生模型。
*   **关键技术细节**：
    *   **历史嵌入蒸馏（Feature Distillation）**：使用一个轻量级模块（3层全连接网络），根据当前影像的嵌入向量（Embedding）预测缺失的历史影像嵌入，并使用均方误差（MSE）损失进行约束。
    *   **多教师蒸馏方案（Multi-Teacher Distillation）**：针对 1 到 5 年的每个预测跨度（Horizon），分别训练一个专门的“教师专家”。这些教师模型输入完整的真实历史序列，专注于优化特定年份的预测。
    *   **学生模型训练**：学生模型输入“当前影像 + 重构的历史嵌入”，通过 KL 散度损失函数吸收多个教师模型输出的逻辑值（Logits）。
    *   **损失函数组合**：总损失 $L_{total}$ 由加权交叉熵损失（RCE）、特征蒸馏损失（MSE）和逻辑值蒸馏损失（KL）组成。
    *   **架构无关性**：PHD 框架可适配不同的纵向聚合器（如基于 Transformer 的 LoMaR 或基于 Mamba 的 VMRA）。

#### 3. 实验设计
*   **数据集**：使用大规模纵向乳腺摄影数据集 **CSAW-CC**（包含 7,353 名患者的 19,328 次检查，其中 1,413 例在未来确诊癌症）。
*   **Benchmark 与对比方法**：
    *   **Mirai**：最先进的单次影像风险预测模型。
    *   **LoMaR**：基于 Transformer 的纵向基准模型。
    *   **VMRA**：基于 Vision-Mamba 的最新纵向基准模型。
    *   **对比场景**：全历史输入（#H=4）、无历史输入（#H=0）以及应用 PHD 后的无历史输入（#H=0+）。
*   **评估指标**：时间依赖性 AUC 和低误报率下的部分 AUC（pAUC@10% FPR）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用 Adam 优化器，初始学习率为 $10^{-3}$，采用余弦退火调度，训练最多 30 个 Epoch，并设有早停机制（5 个 Epoch 无提升则停止）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **10 次随机患者级划分**以确保结果的统计稳定性。
    *   在测试集验证时，对每个患者进行 **100 次重复采样**以消除跨度偏差。
    *   **消融实验**：对比了“无蒸馏”、“单教师蒸馏”与“多教师蒸馏”的效果。
*   **充分性评价**：实验设计较为充分，涵盖了主流的纵向模型架构，并通过统计显著性测试（p-value）验证了改进的有效性。对比实验在“有无历史”的多种组合下展开，具有较强的客观性。

#### 6. 主要结论与发现
*   **性能跨越**：在推理时完全不使用历史影像的情况下，PHD 显著提升了长周期（4-5年）的预测性能。例如，VMRA+PHD 在 5 年期的 pAUC 从 0.710 提升至 0.757。
*   **比肩全历史**：PHD 模型的表现不仅远超无历史的基准模型，甚至在低误报率区域（临床关注重点）达到或超过了拥有完整历史记录的模型性能。
*   **多教师的必要性**：消融实验证明，针对不同预测跨度设置独立教师比单一教师更能有效传递复杂的纵向风险信号。

#### 7. 优点
*   **临床实用性极高**：解决了纵向模型在实际部署中因数据不全而无法使用的痛点。
*   **灵活性强**：作为一种训练策略，它可以无缝集成到现有的各种深度学习纵向架构中。
*   **低 FPR 表现优异**：在低误报率下具有更高的灵敏度，这对于减少不必要的活检和患者焦虑至关重要。

#### 8. 不足与局限
*   **依赖预训练编码器**：实验中使用了冻结的 Mirai 图像编码器，如果底层编码器无法捕捉某些细微特征，蒸馏的效果可能会受限。
*   **数据集单一**：虽然 CSAW-CC 是高质量的大型数据集，但模型在不同种族、不同设备采集的数据集上的泛化能力仍需进一步验证。
*   **历史重构的局限**：特征蒸馏模块仅在嵌入空间进行重构，无法直观解释模型“补全”了哪些具体的临床影像特征。

（完）
