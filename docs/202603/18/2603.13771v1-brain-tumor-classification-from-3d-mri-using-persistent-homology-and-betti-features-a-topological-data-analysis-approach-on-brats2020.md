---
title: "Brain Tumor Classification from 3D MRI Using Persistent Homology and Betti Features: A Topological Data Analysis Approach on BraTS2020"
title_zh: 基于持续同调与 Betti 特征的 3D MRI 脑肿瘤分类：一种针对 BraTS2020 的拓扑数据分析方法
authors: Faisal Ahmed
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13771v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 用于脑肿瘤分类的拓扑数据分析
tldr: "本研究针对3D MRI脑肿瘤分类中的高维复杂结构挑战，提出了一种基于拓扑数据分析（TDA）的分类框架。通过对BraTS 2020数据集的FLAIR序列应用持续同调技术，提取了描述连通分量、环和空腔的Betti数特征。该方法将复杂的3D肿瘤形态简化为100个可解释的拓扑描述符，并结合随机森林等传统机器学习模型，在HGG与LGG分类中实现了89.19%的准确率，为医学影像分析提供了一种高效且具解释性的新途径。"
selection_source: fresh_fetch
motivation: 旨在解决3D MRI影像在高维数据处理和复杂肿瘤结构模式识别中存在的解释性不足及计算复杂性问题。
method: "利用持续同调技术从3D MRI体积中提取Betti数（Betti-0, 1, 2）作为拓扑特征，并使用随机森林和XGBoost分类器进行训练。"
result: "在BraTS 2020数据集上，结合所选Betti特征的随机森林分类器在区分高级别与低级别胶质瘤时达到了89.19%的准确率。"
conclusion: 持续同调作为一种有效的拓扑分析方法，能够以极低的数据维度捕捉复杂的肿瘤形态，为医学影像分类提供了一种兼具性能与解释性的替代方案。
---

## 摘要
由于磁共振成像 (MRI) 中存在的高维性和复杂的结构模式，从医学影像中进行准确且可解释的脑肿瘤分类仍然是一个具有挑战性的问题。在本研究中，我们提出了一种基于拓扑数据分析 (TDA) 的拓扑驱动框架，直接应用于三维 (3D) MRI 体数据进行脑肿瘤分类。具体而言，我们分析了 BraTS 2020 数据集中的 3D 液体衰减反转恢复 (FLAIR) 图像，并利用持续同调提取了可解释的拓扑描述符。持续同调通过 Betti 数捕捉数据的内在几何和结构特征，这些 Betti 数描述了连通分量 (Betti-0)、环 (Betti-1) 和空腔 (Betti-2)。从 3D MRI 体数据中，我们推导出了一组包含 100 个拓扑特征的紧凑集合，总结了脑肿瘤结构的底层拓扑。这些描述符在显著降低数据维度的同时，表征了复杂的 3D 肿瘤形态。与许多需要大规模训练数据或复杂架构的深度学习方法不同，所提出的框架依赖于直接从图像中提取的计算高效的拓扑特征。这些特征被用于训练经典的机器学习分类器，包括随机森林 (Random Forest) 和 XGBoost，以实现高级别胶质瘤 (HGG) 和低级别胶质瘤 (LGG) 的二分类。在 BraTS 2020 数据集上的实验结果表明，结合所选 Betti 特征的随机森林分类器达到了 89.19% 的准确率。这些发现突显了持续同调作为一种有效且可解释的方法，在分析复杂 3D 医学图像和进行脑肿瘤分类方面的潜力。

## Abstract
Accurate and interpretable brain tumor classification from medical imaging remains a challenging problem due to the high dimensionality and complex structural patterns present in magnetic resonance imaging (MRI). In this study, we propose a topology-driven framework for brain tumor classification based on Topological Data Analysis (TDA) applied directly to three-dimensional (3D) MRI volumes. Specifically, we analyze 3D Fluid Attenuated Inversion Recovery (FLAIR) images from the BraTS 2020 dataset and extract interpretable topological descriptors using persistent homology.   Persistent homology captures intrinsic geometric and structural characteristics of the data through Betti numbers, which describe connected components (Betti-0), loops (Betti-1), and voids (Betti-2). From the 3D MRI volumes, we derive a compact set of 100 topological features that summarize the underlying topology of brain tumor structures. These descriptors represent complex 3D tumor morphology while significantly reducing data dimensionality.   Unlike many deep learning approaches that require large-scale training data or complex architectures, the proposed framework relies on computationally efficient topological features extracted directly from the images. These features are used to train classical machine learning classifiers, including Random Forest and XGBoost, for binary classification of high-grade glioma (HGG) and low-grade glioma (LGG). Experimental results on the BraTS 2020 dataset show that the Random Forest classifier combined with selected Betti features achieves an accuracy of 89.19%. These findings highlight the potential of persistent homology as an effective and interpretable approach for analyzing complex 3D medical images and performing brain tumor classification.

---

## 论文详细总结（自动生成）

### 论文总结：基于持续同调与 Betti 特征的 3D MRI 脑肿瘤分类

#### 1. 核心问题与研究动机
*   **核心问题**：如何从高维、复杂的 3D MRI 影像中提取具有**可解释性**且**计算高效**的特征，以实现准确的脑肿瘤（高级别胶质瘤 HGG vs. 低级别胶质瘤 LGG）分类。
*   **研究动机**：
    *   **深度学习的局限性**：传统的 CNN 等深度学习模型虽然性能强劲，但通常被视为“黑盒”，缺乏医学解释性，且对标注数据量和计算资源（GPU）依赖极高。
    *   **拓扑特征的优势**：拓扑数据分析（TDA）能够捕捉形状、连通性、空腔等内在几何结构，这些特征对噪声鲁棒，且对旋转和平移具有不变性，非常适合描述肿瘤复杂的形态学特征。

#### 2. 方法论
该论文提出了一种拓扑驱动的分类框架，核心步骤如下：
*   **数据预处理**：选用 BraTS 2020 的 FLAIR 模态，截取 3D 体积中包含肿瘤核心的中间切片（第 30-90 层），将体素强度归一化至 [0, 255]。
*   **立方体过滤（Cubical Filtration）**：构建子水平集过滤过程。随着灰度阈值 $t$ 的增加，激活相应的体素，形成一系列嵌套的立方体复合物。
*   **持续同调（Persistent Homology）计算**：
    *   跟踪拓扑特征在过滤过程中的“出生”与“死亡”。
    *   提取三个维度的特征：**Betti-0**（连通分量）、**Betti-1**（环/隧道结构）、**Betti-2**（空腔/空洞）。
*   **特征向量化（Betti Curves）**：将持续图转化为固定长度的 Betti 曲线。在 100 个均匀分布的阈值点上采样，每个维度生成 100 维特征，总计 300 维。
*   **特征选择与分类**：利用树模型的特征重要性评分筛选关键拓扑描述符，随后输入**随机森林（Random Forest）**和 **XGBoost** 分类器。

#### 3. 实验设计
*   **数据集**：使用 **BraTS 2020** 挑战赛数据集，包含 369 个病例（293 例 HGG，76 例 LGG）。
*   **实验场景**：二分类任务（HGG vs. LGG）。
*   **对比设置**：
    *   不同拓扑特征维度的对比（单独使用 B0, B1, B2 或其组合）。
    *   有无特征选择（Feature Selection）的对比。
    *   两种经典机器学习模型（RF vs. XGBoost）的性能对比。
*   **评估指标**：准确率（Accuracy）、精确率（Precision）、召回率（Recall）、AUC 和混淆矩阵。

#### 4. 资源与算力
*   **硬件平台**：实验在一台搭载 **Apple M1 芯片**（8 核 CPU，16 GB 统一内存）的个人笔记本电脑上完成。
*   **算力需求**：该方法**无需 GPU**。论文强调了其轻量化特性，证明了在普通消费级硬件上处理 3D 医疗影像的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：针对 5 种不同的特征组合（B0, B1, B2, B1+B2, B0+B1+B2）在两种模型上分别进行了测试，并对比了特征选择前后的效果。
*   **充分性评价**：
    *   **优点**：通过 PCA 降维可视化证明了拓扑特征的区分度；使用了 80/20 的训练/测试划分。
    *   **不足**：由于 LGG 样本较少（76例），实验面临一定的**类别不平衡**风险。虽然结果显示准确率较高，但若能增加交叉验证的折数或使用过采样技术会更具说服力。

#### 6. 主要结论与发现
*   **拓扑特征有效性**：Betti 特征能显著捕捉 HGG 和 LGG 之间的形态差异。HGG 通常表现出更复杂的拓扑结构（更多的连通分量和空腔）。
*   **最优模型**：**随机森林**结合经过筛选的 Betti 特征表现最佳，准确率达到 **89.19%**。
*   **特征选择的重要性**：通过特征选择将维度从 300 维降至 14 维后，模型性能反而有所提升，说明拓扑特征中存在冗余，精简后的特征更具判别力。

#### 7. 优点
*   **高度可解释性**：Betti 数直接对应解剖学上的连通性、环和空腔，方便医生理解模型决策依据。
*   **计算效率高**：相比深度学习，该方法特征维度极低，训练速度快，且不需要复杂的数据增强。
*   **鲁棒性**：拓扑特征对影像采集过程中的轻微噪声和几何变换不敏感。

#### 8. 不足与局限
*   **数据不平衡**：HGG 和 LGG 样本比例约为 4:1，可能导致模型对少数类（LGG）的泛化能力受限。
*   **模态利用不足**：仅使用了 FLAIR 模态，未结合 T1、T2 等其他模态的互补信息。
*   **对比基准缺失**：论文未直接给出与当前最先进（SOTA）的深度学习模型（如 3D U-Net 或 Swin UNETR）在相同数据集上的量化对比。
*   **计算瓶颈**：虽然分类阶段快，但对于超大规模 3D 图像，计算持续同调的初始阶段可能存在内存消耗较高的风险。

（完）
