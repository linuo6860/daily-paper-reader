---
title: Composed Vision-Language Retrieval for Skin Cancer Case Search via Joint Alignment of Global and Local Representations
title_zh: 通过全局与局部表示联合对齐的皮肤癌病例搜索组合式视觉-语言检索
authors: "Yuheng Wang, Yuji Lin, Dongrun Zhu, Jiayue Cai, Sunil Kalia, Harvey Lui, Chunqi Chang, Z. Jane Wang, Tim K. Lee"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09108v1"
tags: ["query:bio-ca"]
score: 9.0
evidence: 皮肤癌病例搜索的视觉语言检索
tldr: 本研究针对皮肤癌病例检索，提出了一种结合图像和文本描述的组合式视觉语言检索框架。该框架基于Transformer，通过联合对齐全局和局部表示来学习层级化的查询特征。局部对齐利用空间注意力掩码聚合判别性区域，全局对齐提供整体语义监督，并采用领域感知的加权方式计算相似度。在Derm7pt数据集上的实验证明，该方法显著优于现有技术，能有效支持临床决策和医学教育。
selection_source: fresh_fetch
motivation: 传统的单一模态检索难以满足临床中结合病灶图像与皮肤镜特征描述进行精确病例搜索的需求。
method: 提出一种基于Transformer的框架，通过空间注意力掩码实现局部对齐，并结合全局语义监督与领域感知加权计算相似度。
result: 在公开的Derm7pt数据集上，该方法在各项检索指标上均一致优于当前最先进的方法。
conclusion: 该框架为皮肤癌病例的高效检索提供了可行方案，有助于临床部署并提升诊断决策的准确性。
---

## 摘要
医学图像检索旨在识别临床相关的病变病例，以支持诊断决策、教育和质量控制。在实践中，检索查询通常结合了参考病变图像与文本描述符（如皮肤镜特征）。我们研究了针对皮肤癌的组合式视觉-语言检索，其中每个查询由图像-文本对组成，数据库包含经活检确认的多类别疾病病例。我们提出了一个基于 Transformer 的框架，该框架学习分层组合查询表示，并在查询与候选图像之间执行联合全局-局部对齐。局部对齐通过多个空间注意力掩码聚合判别性区域，而全局对齐则提供整体语义监督。最终相似度通过一种凸的、结合领域知识的加权方式计算，该方式在保持全局一致性的同时，强调了临床显著的局部证据。在公开数据集 Derm7pt 上的实验表明，该方法相较于现有最先进方法具有持续的改进。所提出的框架能够高效访问相关医疗记录，并支持实际临床部署。

## Abstract
Medical image retrieval aims to identify clinically relevant lesion cases to support diagnostic decision making, education, and quality control. In practice, retrieval queries often combine a reference lesion image with textual descriptors such as dermoscopic features. We study composed vision-language retrieval for skin cancer, where each query consists of an image to text pair and the database contains biopsy-confirmed, multi-class disease cases. We propose a transformer based framework that learns hierarchical composed query representations and performs joint global-local alignment between queries and candidate images. Local alignment aggregates discriminative regions via multiple spatial attention masks, while global alignment provides holistic semantic supervision. The final similarity is computed through a convex, domain-informed weighting that emphasizes clinically salient local evidence while preserving global consistency. Experiments on the public Derm7pt dataset demonstrate consistent improvements over state-of-the-art methods. The proposed framework enables efficient access to relevant medical records and supports practical clinical deployment.

---

## 论文详细总结（自动生成）

以下是对论文《Composed Vision-Language Retrieval for Skin Cancer Case Search via Joint Alignment of Global and Local Representations》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的皮肤癌辅助诊断系统多侧重于端到端的分类，但在临床实践中，医生更倾向于通过检索相似的历史病例（基于图像和描述性文本）来进行对比评估。
*   **研究动机**：现有的检索方法通常仅使用单一的图像或文本查询。本文旨在研究**组合式视觉-语言检索（Composed Image Retrieval, CIR）**，即同时利用“参考病变图像”和“临床文本描述”（如皮肤镜特征）作为查询条件，从数据库中检索经活检确认的相似病例。
*   **背景**：这是首个将组合式检索应用于皮肤癌病例搜索的研究，旨在通过整合多模态信息提高检索的临床相关性和准确性。

### 2. 提出方法论
核心思想是构建一个基于 Transformer 的框架，通过层级化表示学习和全局-局部联合对齐来实现精准匹配。
*   **层级化视觉编码**：使用 **Swin Transformer** 作为骨干网络，提取图像的低、中、高三层特征图，以兼顾细粒度外观和高层语义。
*   **跨模态组合**：利用 **BERT** 编码临床文本，并通过**跨模态 Transformer（Cross-Transformer）**将文本特征注入到参考图像的视觉特征中，生成组合查询表示。
*   **全局-局部联合对齐**：
    *   **局部对齐**：通过多个可学习的空间注意力掩码（Region Masks）捕捉病灶的判别性局部模式（如条纹、不规则色素沉着等），计算局部相似度 $S_{local}$。
    *   **全局对齐**：对特征图进行全局池化，捕捉整体形态和颜色分布，计算全局相似度 $S_{global}$。
*   **相似度计算**：采用凸组合加权公式 $S = \beta S_{local} + (1 - \beta) S_{global}$。研究设定 $\beta=0.6$，通过强调局部临床证据来优化检索排序。

### 3. 实验设计
*   **数据集**：使用公开的 **Derm7pt** 数据集。筛选出样本量大于50的类别，最终包含 888 张图像，涵盖黑色素瘤（mel）、痣（nevus）和良性角化类病变（bkl）三类。
*   **Benchmark 与对比方法**：
    *   **ResNet50-CosSim**：基于深度分类特征的传统图像检索基准。
    *   **SNF-DCA**：该数据集上的首个多模态检索方法。
    *   **MaskRCNN-Fusion**：近期针对皮肤病设计的分割驱动融合检索方法。
    *   **DAHNET**：最新的先进哈希检索研究。
*   **评价指标**：平均精度均值（mAP）和 Accuracy@K（K=1, 2, 4）。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率为 $10^{-4}$，权重衰减为 $10^{-5}$。训练 100 个 epoch，并采用早停策略（patience=30）。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。考虑到模型基于 Swin Transformer 和 BERT，且数据集规模较小（<1000样本），预计在单张消费级显卡（如 RTX 3090）上即可完成训练。

### 5. 实验数量与充分性
*   **实验规模**：采用了 **5 折交叉验证（5-fold cross-validation）** 来评估性能，这在小规模医学数据集上能提供更可靠的统计估计。
*   **充分性与公平性**：
    *   对比了四种不同类型的先进方法（从基础 CNN 到多模态融合再到哈希网络）。
    *   进行了定性分析，展示了前 5 名检索结果的视觉一致性。
    *   **局限性**：实验仅在一个数据集（Derm7pt）上进行，且类别仅限于三类，缺乏在更大规模、更多样化数据集上的泛化性验证。

### 6. 主要结论与发现
*   **性能领先**：所提方法在所有指标上均优于现有 SOTA。Acc@1 达到 **79.3%**，mAP 达到 **81.7%**。
*   **局部特征的重要性**：在 Acc@1 指标上的显著提升表明，强调局部诊断线索（如病灶内部的微小结构）能有效解决视觉相似病灶之间的歧义。
*   **组合查询的优势**：将文本属性（皮肤镜特征）融入查询，比单纯依赖图像外观检索能获得更符合临床逻辑的结果。

### 7. 优点
*   **临床契合度高**：模拟了皮肤科医生结合图像与症状描述进行诊断的真实工作流。
*   **模型设计合理**：通过全局-局部联合对齐，既保证了语义的一致性，又突出了对诊断至关重要的局部细节。
*   **领域知识驱动**：相似度权重的分配（$\beta=0.6$）体现了对皮肤病诊断中局部病理特征重要性的理解。

### 8. 不足与局限
*   **数据集规模有限**：888 张图像对于深度 Transformer 模型来说规模较小，可能存在过拟合风险。
*   **类别覆盖不足**：仅针对三类常见皮肤病，未涵盖更罕见或更复杂的皮肤癌类型。
*   **文本输入受限**：实验中的文本是基于结构化检查表生成的，而非真实的非结构化医生自然语言描述，这可能简化了跨模态理解的难度。
*   **缺乏硬件细节**：未提供算力消耗和推理延迟数据，不利于评估其在移动端或实时临床环境中的部署潜力。

（完）
